services:
  app:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_PRIVY_APP_ID: ${NEXT_PUBLIC_PRIVY_APP_ID}
    restart: unless-stopped
    env_file: .env.production
    ports:
      - "3000:3000"
